/**
 * Search Email Feature Module
 * 
 * Consolidated email search for individual contacts via "Find Email" button.
 * Runs waterfall search (Apollo → Perplexity → Hunter) with single billing point.
 */

export { registerSearchEmailRoutes } from './routes';
